<a href='<%= config.server.url %>'>
  <img width='60%' height='auto' src='<%= config.server.logo.url %>'>
</a>
<br><br>
本人確認URLは以下の通りです。
<br>
<a href="<%= config.email_redirect_url %>"><%= config.email_redirect_url %></a><br>
なお、期限切れとなった場合は、再送信ボタンを押してください。
それでも再度期限切れとなった場合は、以下のサイトよりログインすることで、
メールを再送信できるフォームに移動します。<br>
<a href='<%= config.server.url %><= config.server.root %>/signin'><%= config.server.url %><= config.server.root %>/signin</a><br>
<br><br><br>
もし、このメールが不本意に送られていた場合、
<br>
お手数ですが、こちらのメールに返信してお問い合わせしてくださいませ。
<br><br>
<a href='<%= config.server.url %>'>
  <div style='color:#808080;text-align:center;'>
    <u>Copyright &copy; <%= config.year %> <%= config.name %></u>
  </div>
</a>
